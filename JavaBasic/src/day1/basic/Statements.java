package day1.basic;

public class Statements {

	public static void main(String[] args) {
		/*
		 * Keywords: public, static, void, class, package, int
		 * Identifier: Statements,main(predefined Identifier),args,age,out(predefined Identifier),println(predefined Identifier)
		 * Predefined classes: System, String
		 */
		int age=25;
		int sal=40255;
		//int float=45; //compile time error because float is a Keyword
		int age2=30;
		int Age_1=45;
		int age$2=35;
		//int 3age=29;// compile time error, as we can't start identifier with number
		int age3=45;
		int empAge=45;
		int _Salary=45000;
		int $pincode=411015;
		//int emp Salary=35000; //compile time error, as we can't have whitespace in between identifier
		//int age@Emp=45; //compile time error, as we can't special symbol other then '$' and '_' in identifier
	}

}
